import { LessonFilterPipe } from './lesson-filter.pipe';

describe('LessonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LessonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

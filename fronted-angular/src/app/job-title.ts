export class JobTitle {
    position:string;
    salary:number;
    bonus:boolean;
  constructor(position: string, salary: number, bonus: boolean) {
    this.position = position;
    this.salary = salary;
    this.bonus = bonus;
  }
}
